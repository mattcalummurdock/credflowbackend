# One chain at a time — or use the Python SpokeAgent
npx hardhat run scripts/lz-verify-spoke-scores.js --network arbitrumSepolia
npx hardhat run scripts/lz-verify-spoke-scores.js --network baseSepolia